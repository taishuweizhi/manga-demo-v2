# 漫剧工作流系统设计规划文档

**文档版本**：V1.0  
**编写日期**：2026年04月  
**文档类型**：产品设计规划  
**适用范围**：漫剧创作平台 PRD 前期参考

---

## 1. 产品概述

### 1.1 产品定位

漫剧工作流系统是一款面向创作者的一站式漫剧（漫画+视频）创作管理平台。系统通过 AI 智能解析技术，将剧本自动拆解为分镜，并生成可执行的视频生成指令，实现从文字剧本到视频成品的全流程自动化管理。

### 1.2 核心价值

| 核心价值点 | 描述 |
|-----------|------|
| **效率提升** | 将传统的分镜设计、场景匹配、角色关联等重复性工作交由 AI 自动完成，大幅缩短创作周期 |
| **风格统一** | 通过全局画风配置和默认模型设置，确保项目内所有分镜保持一致的视觉风格 |
| **资源复用** | 角色、场景、物品库支持复用，避免重复创建，降低创作成本 |
| **流程闭环** | 打通剧本解析→分镜生成→视频生成→下载输出的完整链路，实现一站式创作体验 |

### 1.3 目标用户

| 用户类型 | 典型场景 | 核心需求 |
|---------|---------|---------|
| **独立创作者** | 个人漫剧/动画短片创作 | 快速产出、降低门槛、自主演完 |
| **MCN机构** | 系列化内容批量生产 | 流程标准化、团队协作、效率优先 |
| **内容工作室** | 商业项目委托制作 | 品质把控、版本管理、交付规范 |
| **教育培训** | 视频创作教学 | 教学演示、作业管理、模板复用 |

---

## 2. 系统架构

### 2.1 整体工作流架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              漫剧工作流系统                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │   项目管理层  │───▶│   剧本解析层  │───▶│   智能分析层  │───▶│   视频生成层  │  │
│  └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│         │                  │                  │                  │         │
│         ▼                  ▼                  ▼                  ▼         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          资源管理层（角色/场景/物品）                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心模块划分

| 模块层级 | 模块名称 | 职责描述 |
|---------|---------|---------|
| **表现层** | UI/UX交互界面 | 用户操作入口，状态可视化展示 |
| **业务层** | 项目管理模块 | 项目创建、配置、列表管理 |
| **业务层** | 剧本输入模块 | 文件上传、格式解析、内容提取 |
| **业务层** | 智能分析模块 | 角色/场景/物品识别，分镜拆分 |
| **业务层** | 分镜管理模块 | 分镜列表、Prompt生成、时长计算 |
| **业务层** | 视频生成模块 | 视频生成指令下发、状态跟踪、下载管理 |
| **业务层** | 资源管理模块 | 角色库、场景库、物品库维护 |
| **支撑层** | AI模型服务 | 图片生成模型、多模态理解模型调用 |
| **支撑层** | 数据存储层 | 项目数据、分镜数据、资源数据持久化 |

---

## 3. 功能模块详细设计

### 3.1 项目管理模块

#### 3.1.1 创建项目

**功能描述**：用户创建新的漫剧项目，设置项目基础信息和全局生成偏好。

**入口**：项目列表页面 → 「创建项目」按钮

**弹窗字段**：

| 字段名称 | 类型 | 约束 | 说明 |
|---------|------|------|------|
| 项目名称 | 文本输入 | 0-100字符 | 项目标识名称，必填 |
| 默认图片模型 | 下拉选择 | - | 用于生成、垫图等图片类操作，默认：`doubao-widebeam-0.5` |
| 默认多模态模型 | 下拉选择 | - | 用于图文结合的多模态内容，默认：`happyprompt-1.0` |
| 默认画风 | 下拉选择 | - | 全局风格指定，确保输出统一，默认：`默认二次元插画风格` |
| 默认负向比例 | 数值滑块 | 0.00-1.00 | 负向权重控制，默认：0.10 |
| 默认场强比例 | 数值滑块 | 0.00-1.00 | 场强权重控制，默认：0.10 |
| 默认裁切比例 | 数值滑块 | 0.00-1.00 | 裁切优化控制，默认：0% |
| 默认模糊比例 | 数值滑块 | 0.00-1.00 | 模糊处理控制，默认：15% |

**交互说明**：
- 点击「取消」关闭弹窗，不保存任何更改
- 点击「创建项目」验证必填字段，验证通过后创建项目并跳转至项目详情页

#### 3.1.2 项目列表

**功能描述**：管理所有已创建的项目，支持切换、删除等操作。

**界面元素**：
- 项目卡片/列表项：显示项目名称、创建时间、剧本数量
- 「创建项目」按钮：新增项目入口

### 3.2 剧本输入与解析模块

#### 3.2.1 文件上传

**功能描述**：用户上传剧本文件，系统自动解析提取剧本内容。

**入口**：项目详情页 → 上传区域 / 上传按钮

**支持格式**：
| 格式 | MIME类型 | 说明 |
|-----|---------|------|
| `.txt` | text/plain | 纯文本格式 |
| `.docx` | application/vnd.openxmlformats-officedocument.wordprocessingml.document | Word文档格式 |

**交互流程**：
1. 点击上传区域或按钮，弹出系统文件选择窗口
2. 筛选文件类型：`*.txt;*.docx`
3. 选择文件后自动触发上传
4. 上传完成后自动启动剧本分析流程

**异常处理**：
- 文件大小超限：提示「文件过大，请上传不超过 XX MB 的文件」
- 格式不支持：提示「暂不支持该文件格式，请上传 .txt 或 .docx 文件」
- 解析失败：提示「剧本解析失败，请检查文件内容格式后重试」

### 3.3 智能分析模块

#### 3.3.1 分析流程

**触发时机**：剧本文件上传完成后自动启动

**分析阶段划分**：

```
┌─────────────────┐
│  1. 项目解析     │  ──▶  "项目分析进行中，请稍候"
└────────┬────────┘
         ▼
┌─────────────────┐
│  2. 题集拆分     │  ──▶  "题集已拆分完成，正在分析角色、物品、场景并准备分镜，请稍候"
└────────┬────────┘
         ▼
┌─────────────────┐
│  3. 细化分析     │  ──▶  "当前剧集分镜分析中，请稍候刷新查看结果"
└────────┬────────┘
         ▼
┌─────────────────┐
│  4. 分镜生成     │  ──▶  "当前图集分镜分析中"
└────────┬────────┘
         ▼
    分析完成
```

#### 3.3.2 角色识别

**功能描述**：AI 自动识别剧本中的角色，并建立角色库关联。

**识别内容**：
- 角色名称（如：阿橘、太奶/太后、大乔、周瑜）
- 角色首次出现的场景
- 角色对话/旁白频次统计

**输出结果**：角色列表 → 关联至「角色」标签页

#### 3.3.3 场景识别

**功能描述**：AI 自动识别剧本中描述的场景，并建立场景库关联。

**识别内容**：
- 场景名称（如：卧室、梦境空间）
- 场景特征描述
- 场景切换时机

**输出结果**：场景列表 → 关联至「场景」标签页

#### 3.3.4 物品识别

**功能描述**：AI 自动识别剧本中的关键物品/道具。

**识别内容**：
- 物品名称（如：帕子）
- 物品出现位置
- 物品视觉特征描述

**输出结果**：物品列表 → 关联至「物品」标签页

#### 3.3.5 分镜拆分

**功能描述**：将剧本内容智能拆分为独立分镜单元。

**拆分依据**：
- 场景切换
- 角色切换
- 时间推进
- 情绪转折

**输出结果**：分镜列表（含编号、原文内容、关联信息）

### 3.4 分镜生成与管理模块

#### 3.4.1 分镜表格展示

**展示入口**：「展开剧本分析」展开后显示

**表格列定义**：

| 列名 | 数据类型 | 说明 |
|-----|---------|------|
| 编号 | 整数 | 分镜序号，从1开始递增 |
| 原文内容 | 长文本 | 剧本原文对白/旁白内容 |
| 关联角色 | 标签列表 | 该分镜涉及的角色名称 |
| 关联场景 | 标签 | 该分镜发生的场景 |
| 关联物品 | 标签列表 | 该分镜涉及的关键物品 |
| 时长 | 数值（秒） | 自动计算的预估时长（4s、5s、8s等） |
| 视频预览 | 缩略图/占位 | 视频生成后的预览区域 |
| 视频备注 | 文本 | 用户可编辑的备注信息 |
| 分镜prompt | 结构化文本 | AI自动生成的详细分镜描述 |
| 操作 | 操作按钮组 | 视频生成/下载视频/删除分镜 |

#### 3.4.2 分镜Prompt结构

**Prompt 模板结构**：

```
【分镜Prompt结构】
├── 口型指示
│   └── 示例："全程人物无对白口型，嘴巴不动"
├── 视频分镜配置
│   ├── 总分镜数量
│   └── 分镜组成说明
├── 分镜1 (0-4s)
│   ├── 时间/光线
│   │   └── 示例："白天，光线柔和"
│   ├── 场景图片
│   │   └── 示例："场景图片：卧室"
│   ├── 画面描述
│   │   └── 示例："阿橘躺在床上，闭着眼睛，表情平静，进入梦乡"
│   └── 镜头描述
└── 分镜N (时间区间)
    └── 同上结构
```

#### 3.4.3 时长计算规则

**自动计算依据**：
| 内容类型 | 基础时长 | 备注 |
|---------|---------|------|
| 动作场景 | 4-6秒 | 快节奏切换 |
| 对话场景 | 5-8秒 | 根据台词长度调整 |
| 情绪场景 | 8-10秒 | 留足情绪铺垫 |
| 转场 | 2-3秒 | 场景切换过渡 |

#### 3.4.4 新建分镜

**功能描述**：手动新增分镜，补充或调整剧本分析结果。

**入口**：分镜表格顶部 → 「新建分镜」按钮

### 3.5 视频生成模块

#### 3.5.1 视频生成

**触发方式**：
- 单条分镜：点击分镜行「操作」列的「视频生成」按钮
- 批量生成：（可选扩展功能）选中多条分镜后批量触发

**状态流转**：

```
[待生成] ──▶ [生成中] ──▶ [生成成功] ──▶ [可下载]
                │
                └──▶ [生成失败] ──▶ [可重试]
```

**界面反馈**：
- 生成中：按钮变为「生成中...」状态，显示加载动画
- 成功：视频预览区域显示缩略图
- 失败：显示错误提示，支持「重新分析」或「重试」

#### 3.5.2 视频下载

**入口**：分镜行「操作」列的「下载视频」按钮

**下载条件**：视频生成状态为「成功」

**异常处理**：
- 视频不存在：按钮禁用或提示「视频尚未生成」
- 下载超时：提示「下载失败，请重试」

#### 3.5.3 重新分析

**功能描述**：对已分析的分镜重新执行 AI 分析流程。

**入口**：分镜行「操作」列的「重新分析」按钮

**触发场景**：
- 生成的视频效果不理想
- 剧本内容有修改
- Prompt需要优化

### 3.6 资源管理模块

#### 3.6.1 全局设置

**功能描述**：项目的全局配置入口，影响整个项目的生成行为。

**入口**：顶部导航栏 → 「全局设置」标签

**配置项**：同创建项目的全局生成偏好设置

#### 3.6.2 角色管理

**功能描述**：维护项目中的角色库。

**入口**：顶部导航栏 → 「角色」标签

**功能点**：
- 查看已识别角色列表
- 新增/编辑/删除角色
- 角色详情：名称、头像、描述、关联分镜数

#### 3.6.3 场景管理

**功能描述**：维护项目中的场景库。

**入口**：顶部导航栏 → 「场景」标签

**功能点**：
- 查看已识别场景列表
- 新增/编辑/删除场景
- 场景详情：名称、描述、参考图片、关联分镜数

#### 3.6.4 物品管理

**功能描述**：维护项目中的物品/道具库。

**入口**：顶部导航栏 → 「物品」标签

**功能点**：
- 查看已识别物品列表
- 新增/编辑/删除物品
- 物品详情：名称、描述、参考图片、关联分镜数

---

## 4. 工作流流程

### 4.1 主流程：从创建项目到视频产出

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                              漫剧创作完整工作流                                  │
├────────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐ │
│  │ 步骤1    │    │ 步骤2    │    │ 步骤3    │    │ 步骤4    │    │ 步骤5    │ │
│  │ 创建项目 │───▶│ 上传剧本 │───▶│ AI分析  │───▶│ 审核调整 │───▶│ 视频生成 │ │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘    └────┬─────┘ │
│       │              │              │              │                   │     │
│       │              │              │              │                   ▼     │
│       │              │              │              │            ┌──────────┐ │
│       │              │              │              │            │ 步骤6    │ │
│       │              │              │              │            │ 下载交付 │ │
│       │              │              │              │            └──────────┘ │
│       │              │              │              │                               │
│       ▼              ▼              ▼              ▼                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                            并行资源管理工作流                             │   │
│  │  ┌─────────┐    ┌─────────┐    ┌─────────┐                               │   │
│  │  │ 角色管理 │    │ 场景管理 │    │ 物品管理 │                               │   │
│  │  └─────────┘    └─────────┘    └─────────┘                               │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                │
└────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 各步骤详细说明

#### 步骤1：创建项目

| 项目 | 说明 |
|-----|------|
| 操作 | 点击「创建项目」→ 填写项目名称 → 配置全局生成偏好 → 点击「创建项目」 |
| 输入 | 项目名称、全局参数配置 |
| 输出 | 新建项目，进入项目详情页 |
| 时长 | 即时完成 |

#### 步骤2：上传剧本

| 项目 | 说明 |
|-----|------|
| 操作 | 在项目详情页点击上传区域 → 选择文件（.txt/.docx）→ 确认上传 |
| 输入 | 剧本文件 |
| 输出 | 文件上传成功，进入分析阶段 |
| 时长 | 视文件大小而定，通常 < 5秒 |

#### 步骤3：AI分析

| 项目 | 说明 |
|-----|------|
| 操作 | 系统自动执行，无需用户干预 |
| 子阶段 | 项目解析 → 题集拆分 → 细化分析 → 分镜生成 |
| 输出 | 分镜表格（含原文内容、关联信息、Prompt） |
| 时长 | 约 60-120 秒（视剧本长度而定） |

#### 步骤4：审核调整

| 项目 | 说明 |
|-----|------|
| 操作 | 查看分镜分析结果 → 修正关联关系 → 编辑Prompt → 补充/删除分镜 |
| 输入 | 分镜表格 |
| 输出 | 确认的分镜列表 |
| 时长 | 用户自主控制 |

#### 步骤5：视频生成

| 项目 | 说明 |
|-----|------|
| 操作 | 选择分镜 → 点击「视频生成」→ 等待生成完成 |
| 输入 | 分镜Prompt |
| 输出 | 生成的视频文件 |
| 时长 | 视视频长度和服务器负载而定 |

#### 步骤6：下载交付

| 项目 | 说明 |
|-----|------|
| 操作 | 点击「下载视频」→ 保存至本地 |
| 输入 | 视频文件 |
| 输出 | 可交付的视频成品 |
| 时长 | 即时（下载时间视网络状况） |

---

## 5. 核心数据结构

### 5.1 实体关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Project   │       │   Script    │       │   Storyboard│
│    项目     │──1:N──│    剧本      │──1:N──│    分镜      │
└─────────────┘       └─────────────┘       └──────┬──────┘
       │                                          │
       │ N                                        │ N
       ▼                                          ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Resource  │       │   Resource  │       │   Resource  │
│    角色      │       │    场景      │       │    物品      │
└─────────────┘       └─────────────┘       └─────────────┘
```

### 5.2 数据结构详细定义

#### 5.2.1 Project（项目）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 项目唯一标识 |
| `name` | VARCHAR(100) | 是 | 项目名称 |
| `created_at` | DATETIME | 是 | 创建时间 |
| `updated_at` | DATETIME | 是 | 最后更新时间 |
| `default_image_model` | VARCHAR(50) | 是 | 默认图片模型 |
| `default_multimodal_model` | VARCHAR(50) | 是 | 默认多模态模型 |
| `default_style` | VARCHAR(100) | 是 | 默认画风 |
| `default_negative_ratio` | DECIMAL(3,2) | 是 | 默认负向比例，范围 0.00-1.00 |
| `default_field_ratio` | DECIMAL(3,2) | 是 | 默认场强比例，范围 0.00-1.00 |
| `default_crop_ratio` | DECIMAL(3,2) | 是 | 默认裁切比例，范围 0.00-1.00 |
| `default_blur_ratio` | DECIMAL(3,2) | 是 | 默认模糊比例，范围 0.00-1.00 |
| `status` | ENUM | 是 | 项目状态：draft/active/completed/archived |

#### 5.2.2 Script（剧本）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 剧本唯一标识 |
| `project_id` | UUID | 是 | 所属项目ID，外键 |
| `name` | VARCHAR(100) | 是 | 剧本文件名 |
| `file_path` | VARCHAR(500) | 是 | 文件存储路径 |
| `file_type` | ENUM | 是 | 文件类型：txt/docx |
| `file_size` | BIGINT | 是 | 文件大小（字节） |
| `content` | TEXT | 是 | 剧本文本内容 |
| `created_at` | DATETIME | 是 | 上传时间 |
| `analysis_status` | ENUM | 是 | 分析状态：pending/analyzing/completed/failed |
| `analysis_started_at` | DATETIME | 否 | 分析开始时间 |
| `analysis_completed_at` | DATETIME | 否 | 分析完成时间 |
| `analysis_error` | TEXT | 否 | 分析失败错误信息 |

#### 5.2.3 Storyboard（分镜）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 分镜唯一标识 |
| `script_id` | UUID | 是 | 所属剧本ID，外键 |
| `sequence` | INT | 是 | 分镜序号，从1开始 |
| `original_content` | TEXT | 是 | 剧本原文内容 |
| `duration` | INT | 是 | 预估时长（秒） |
| `video_url` | VARCHAR(500) | 否 | 生成视频的URL |
| `video_status` | ENUM | 是 | 视频状态：pending/generating/success/failed |
| `video_error` | TEXT | 否 | 视频生成错误信息 |
| `video_thumbnail` | VARCHAR(500) | 否 | 视频缩略图URL |
| `remark` | TEXT | 否 | 用户备注 |
| `prompt` | JSON | 是 | 分镜Prompt结构化数据 |
| `created_at` | DATETIME | 是 | 创建时间 |
| `updated_at` | DATETIME | 是 | 更新时间 |

#### 5.2.4 StoryboardPrompt（分镜Prompt - JSON结构）

```json
{
  "lip_sync": "全程人物无对白口型，嘴巴不动",
  "total_shots": 2,
  "shot_composition": "2个分镜",
  "shots": [
    {
      "shot_index": 1,
      "time_range": "0-4s",
      "time_lighting": "白天，光线柔和",
      "scene_image": "卧室",
      "description": "阿橘躺在床上，闭着眼睛，表情平静，进入梦乡",
      "camera": "固定镜头，正面中景"
    },
    {
      "shot_index": 2,
      "time_range": "4-8s",
      "time_lighting": "虚幻，朦胧，光线昏暗",
      "scene_image": "梦境空间",
      "description": "梦境场景描述...",
      "camera": "推镜头..."
    }
  ]
}
```

#### 5.2.5 Character（角色）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 角色唯一标识 |
| `project_id` | UUID | 是 | 所属项目ID，外键 |
| `name` | VARCHAR(50) | 是 | 角色名称 |
| `description` | TEXT | 否 | 角色描述 |
| `avatar_url` | VARCHAR(500) | 否 | 角色头像图片URL |
| `first_appearance` | INT | 否 | 首次出现的分镜序号 |
| `dialogue_count` | INT | 否 | 对话出现次数 |
| `created_at` | DATETIME | 是 | 创建时间 |
| `updated_at` | DATETIME | 是 | 更新时间 |

#### 5.2.6 Scene（场景）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 场景唯一标识 |
| `project_id` | UUID | 是 | 所属项目ID，外键 |
| `name` | VARCHAR(100) | 是 | 场景名称 |
| `description` | TEXT | 否 | 场景特征描述 |
| `image_url` | VARCHAR(500) | 否 | 场景参考图片URL |
| `first_appearance` | INT | 否 | 首次出现的分镜序号 |
| `created_at` | DATETIME | 是 | 创建时间 |
| `updated_at` | DATETIME | 是 | 更新时间 |

#### 5.2.7 Item（物品）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 物品唯一标识 |
| `project_id` | UUID | 是 | 所属项目ID，外键 |
| `name` | VARCHAR(100) | 是 | 物品名称 |
| `description` | TEXT | 否 | 物品视觉特征描述 |
| `image_url` | VARCHAR(500) | 否 | 物品参考图片URL |
| `first_appearance` | INT | 否 | 首次出现的分镜序号 |
| `created_at` | DATETIME | 是 | 创建时间 |
| `updated_at` | DATETIME | 是 | 更新时间 |

#### 5.2.8 StoryboardResource（分镜资源关联表）

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `id` | UUID | 是 | 关联记录唯一标识 |
| `storyboard_id` | UUID | 是 | 分镜ID，外键 |
| `resource_type` | ENUM | 是 | 资源类型：character/scene/item |
| `resource_id` | UUID | 是 | 资源ID，外键 |
| `created_at` | DATETIME | 是 | 关联时间 |

---

## 6. AI模型配置体系

### 6.1 模型类型划分

| 模型类型 | 用途 | 调用场景 |
|---------|------|---------|
| **图片生成模型** | 生成单张图片 | 场景图、物品图、角色参考图 |
| **多模态模型** | 理解图文内容 | 剧本解析、角色识别、场景识别、Prompt生成 |
| **视频生成模型** | 生成视频内容 | 分镜视频生成 |

### 6.2 模型配置参数

#### 6.2.1 图片模型配置

| 参数名称 | 类型 | 默认值 | 说明 |
|---------|------|-------|------|
| `model_name` | STRING | `doubao-widebeam-0.5` | 模型标识名 |
| `negative_ratio` | FLOAT | 0.10 | 负向权重，用于画面优化 |
| `field_ratio` | FLOAT | 0.10 | 场强权重，影响画面构图 |
| `crop_ratio` | FLOAT | 0.00 | 裁切比例，优化画面比例 |
| `blur_ratio` | FLOAT | 0.15 | 模糊比例，画面虚化处理 |

#### 6.2.2 多模态模型配置

| 参数名称 | 类型 | 默认值 | 说明 |
|---------|------|-------|------|
| `model_name` | STRING | `happyprompt-1.0` | 模型标识名 |
| `max_tokens` | INT | 2048 | 最大输出token数 |
| `temperature` | FLOAT | 0.7 | 生成随机性控制 |

#### 6.2.3 画风配置

| 参数名称 | 类型 | 可选值示例 | 说明 |
|---------|------|-----------|------|
| `style` | STRING | `默认二次元插画风格` | 全局画风设定 |
| `style_description` | TEXT | - | 画风详细描述 |

### 6.3 配置作用域

```
┌─────────────────────────────────────────────────────────────┐
│                        项目级配置                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ default_image_model / default_multimodal_model        │ │
│  │ default_style / default_*_ratio                        │ │
│  └───────────────────────────────────────────────────────┘ │
│                           │                                 │
│              可被下层配置覆盖（扩展预留）                     │
│                           ▼                                 │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                    剧本级配置（扩展预留）                │ │
│  └───────────────────────────────────────────────────────┘ │
│                           │                                 │
│                           ▼                                 │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                    分镜级配置（扩展预留）                │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. UI/UX设计要点

### 7.1 界面布局

#### 7.1.1 整体布局结构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              顶部导航栏                                   │
│  ┌─────────┐   ┌─────────────────────────────────────────────────────┐  │
│  │ 项目列表 │   │  全局设置  │  角色  │  场景  │  物品  │  展开剧本分析  │  │
│  └─────────┘   └─────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────┐  ┌─────────────────────────────────┐  │
│  │                               │  │                                 │  │
│  │      剧本对话内容区域           │  │         分镜表格区域             │  │
│  │                               │  │                                 │  │
│  │  ┌─────────────────────────┐  │  │  编号 │ 原文 │ 角色 │ 场景 │ ... │  │
│  │  │ 编号 │ 原文内容 │ 关联角色│  │  │  ───  │  ───  │  ───  │  ─── │     │  │
│  │  └─────────────────────────┘  │  │                                 │  │
│  │                               │  │  操作：视频生成 │ 下载 │ 重新分析 │  │
│  │                               │  │                                 │  │
│  └───────────────────────────────┘  └─────────────────────────────────┘  │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                              状态栏/提示区域                              │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 7.1.2 创建项目弹窗布局

```
┌───────────────────────────────────────────────────────────────┐
│                          创建新项目                             │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  项目名称                                                      │
│  ┌───────────────────────────────────────────────────────┐   │
│  │ 0/100                                                 │   │
│  └───────────────────────────────────────────────────────┘   │
│                                                               │
│  全局生成偏好                                                  │
│  ┌─────────────────┐  ┌─────────────────┐                   │
│  │ 默认图片模型     │  │ 默认多模态模型   │                   │
│  │ doubao-widebeam │  │ happyprompt-1.0 │                   │
│  └─────────────────┘  └─────────────────┘                   │
│  ┌─────────────────┐  ┌─────────────────┐                   │
│  │ 默认画风         │  │ 默认负向比例    │                   │
│  │ 默认二次元...   │  │ ▓▓▓░░░░ 0.10   │                   │
│  └─────────────────┘  └─────────────────┘                   │
│  ┌─────────────────┐  ┌─────────────────┐                   │
│  │ 默认场强比例     │  │ 默认裁切比例    │                   │
│  │ ▓▓░░░░░░ 0.10   │  │ ░░░░░░░░ 0%    │                   │
│  └─────────────────┘  └─────────────────┘                   │
│  ┌─────────────────┐                                        │
│  │ 默认模糊比例     │                                        │
│  │ ▓▓▓░░░░░ 15%    │                                        │
│  └─────────────────┘                                        │
│                                                               │
├───────────────────────────────────────────────────────────────┤
│                        取消    创建项目                        │
└───────────────────────────────────────────────────────────────┘
```

### 7.2 交互流程设计

#### 7.2.1 状态反馈机制

| 操作阶段 | UI反馈 | 说明 |
|---------|-------|------|
| **初始状态** | 空列表 + 上传引导 | 引导用户上传剧本 |
| **上传中** | 进度条 + 上传动画 | 显示上传进度 |
| **分析中** | 加载动画 + 状态文字 | 多阶段状态提示 |
| **分析完成** | 分镜表格 + 成功提示 | 展示分析结果 |
| **生成中** | 按钮loading + 动画 | 单条/批量生成状态 |
| **生成成功** | 缩略图 + 可下载状态 | 可预览、可下载 |
| **生成失败** | 错误提示 + 重试按钮 | 支持重新生成 |

#### 7.2.2 加载状态文案

| 分析阶段 | 显示文案 |
|---------|---------|
| 初始解析 | "项目分析进行中，请稍候" |
| 拆分完成 | "题集已拆分完成，正在分析角色、物品、场景并准备分镜，请稍候" |
| 剧集分析 | "当前剧集分镜分析中，请稍候刷新查看结果" |
| 图集分析 | "当前图集分镜分析中" |

### 7.3 核心交互原则

| 原则 | 具体要求 |
|-----|---------|
| **即时反馈** | 每个操作都有明确的视觉反馈，加载状态不可留白 |
| **进度可见** | 长流程展示分阶段进度，让用户了解当前状态 |
| **操作明确** | 按钮文案清晰，如「视频生成」而非含糊的「执行」 |
| **容错友好** | 失败状态提供明确的错误原因和重试入口 |
| **数据关联可见** | 分镜与角色/场景/物品的关联关系直观展示 |
| **批量操作便捷** | 支持多选分镜后批量操作，提高效率 |

### 7.4 响应式设计要点

| 断点 | 布局适配 |
|-----|---------|
| **≥1200px** | 左右分栏布局：左侧剧本，右侧分镜表 |
| **768-1199px** | 上下布局：剧本区收起为标签，分镜表全宽 |
| **<768px** | 全屏单列：Tab切换剧本/分镜视图 |

---

## 附录

### 附录A：术语表

| 术语 | 定义 |
|-----|------|
| **漫剧** | 融合漫画静态画面与视频动态表现的创作形式 |
| **分镜** | 将剧本内容拆分为独立镜头单元，包含画面描述和时长 |
| **Prompt** | 用于AI模型生成的指令文本 |
| **口型指示** | 指定角色说话/不说话的口型状态 |

### 附录B：文件命名规范

| 文件类型 | 命名格式示例 |
|---------|-------------|
| 剧本文件 | `剧本.txt`、`测试剧本.docx` |
| 分镜相关 | `分镜剧本.txt`、`分镜提示词.txt` |

### 附录C：后续迭代方向（参考）

> 以下内容为基于当前功能的合理扩展方向，不作为本期必做项：

1. **团队协作**：支持多用户协作、权限管理
2. **版本管理**：剧本/分镜版本历史与回滚
3. **模板市场**：预置剧本模板和分镜模板
4. **批量生成**：一次性生成多个分镜视频
5. **自定义模型**：支持用户配置自有AI模型
6. **导出功能**：支持导出分镜表为PDF/Excel格式

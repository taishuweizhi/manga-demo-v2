# 漫剧工作流DEMO v12.1 产品需求文档（PRD）

| 项目 | 内容 |
|------|------|
| 文档版本 | v12.1 |
| 创建日期 | 2026-05-15 |
| 作者 | 刘浏 |
| 审核人 | 待定 |
| 状态 | 草稿 |

---

## 文档说明

本文档中用以下标签区分功能可见性：

| 标签 | 含义 |
|------|------|
| 【DEMO可见】 | 用户能在DEMO页面上看到的交互和界面 |
| 【系统自动】 | 后台自动完成，DEMO不单独展示，但属于产品设计的重要逻辑 |
| 【DEMO模拟】 | DEMO中用模拟数据/延时模拟的功能，非真实后端 |

---

## 一、项目背景与目标

### 1.1 背景

当前漫剧工作流为6步线性流程：剧本列表 → 剧本解析 → 解析详情（角色/场景/道具）→ 分镜列表 → 融图管理 → 生成视频。

在实际生产中，该流程存在多个阻断性问题，导致无法完整生成可用的视频成品：

1. **剧本→分镜的清洗缺失**：原始剧本直接交给AI拆分镜，AI通过提示词处理分镜时出现严重的动作丢失，且分镜与融图对应不上。
2. **流程冗长**：6步流程中融图管理与生成视频职责重叠，用户操作路径不清晰。
3. **视频生成仅支持首帧生成**：单图→视频，只有首帧控制，后续帧不可控，成片质量无法满足生产要求。
4. **视频生成数量设计为"抽卡"逻辑**：下拉选1-4个同时生成，本质是赌概率，成本不透明，用户体验差。

### 1.2 v12.1 优化目标

| 优化项 | 目标 |
|--------|------|
| 精简流程 | 6步→4步（原始剧本→剧集面板→分镜列表→生成视频），去掉融图步骤 |
| 清洗后台自动 | 剧本清洗在后台自动完成，用户无需手动操作清洗结果 |
| 资产选定即出图 | 资产选择后直接展示该资产的图片，可放大查看、删除、替换 |
| 合并查看+编辑 | 详情弹窗整合查看与编辑，左侧编辑分镜内容，右侧配置视频参数 |
| 生成历史弹窗化 | 历史记录通过弹窗展示，避免内联展开导致高度不可控 |
| 资产标签直达编辑 | 分镜列表中点击资产标签，直接弹出该资产的编辑弹窗（不经过库列表） |

---

## 二、新版流程总览

### 2.1 4步流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           漫剧工作流 v12.1                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌────────────┐ │
│  │   步骤1      │    │   步骤2      │    │   步骤3      │    │   步骤4   │ │
│  │  原始剧本    │───▶│  剧集面板    │───▶│  分镜列表    │───▶│ 生成视频  │ │
│  └──────────────┘    └──────────────┘    └──────────────┘    └────────────┘ │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ 核心交互变更：                                                           │  │
│  │ • 清洗后台自动，用户点击"AI剧本分析"后自动完成                            │  │
│  │ • 资产选定即出图（角色/场景/道具选择后展示图片）                          │  │
│  │ • 分镜详情弹窗（detailModal）合并查看+编辑+多参配置                       │  │
│  │ • 资产标签点击直接弹出编辑弹窗（不经过库列表）                            │  │
│  │ • 生成历史通过弹窗展示（historyModal）                                   │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 流程对比

| 维度 | 旧版（v1.3） | 新版（v12.1） |
|------|-------------|---------------|
| 步骤数量 | 6步 | 4步 |
| 融图管理 | 独立步骤 | **移除**（不需要融图流程） |
| 分镜图片 | 独立步骤 | **移除**（不需要单独的分镜图片步骤） |
| 视频生成方式 | 首帧/首尾帧选择 | **直接多参生成** |
| 剧本清洗 | 需用户预览确认 | 后台自动完成 |
| 分镜编辑 | 查看弹窗+编辑弹窗（两个） | detailModal（一个弹窗搞定） |
| 资产选择 | 从列表中选择 | 选定即出图（可放大/删除/替换） |
| 资产编辑入口 | 需进入库列表 | 分镜列表标签点击直达 |
| 生成历史 | 内联展开 | 弹窗展示 |

---

## 三、各步骤功能详述

### 3.1 步骤1：原始剧本

#### 3.1.1 功能描述

【DEMO可见】展示剧本原文，支持选择剧本类型和画面比例，触发AI剧本分析。

#### 3.1.2 页面布局

【DEMO可见】
- 固定高度300px剧本展示区（内置滚动）
- 剧本类型下拉：解说剧 / 仿真人短剧 / 2D动态漫剧
- 画面比例下拉：9:16 / 16:9 / 1:1
- "AI剧本分析"按钮

#### 3.1.3 交互流程

【DEMO可见 + 系统自动】

```
用户点击"AI剧本分析"
    │
    ▼
【DEMO可见】展示进度动画
    │
    ├── 正在清洗剧本...
    ├── 正在拆分镜...
    ├── 正在生成剧集...
    └── 分析完成！
    │
    ▼
【系统自动】后台执行以下逻辑：
    │
    ├── 1. 根据剧本类型映射清洗规则
    │       ├── 解说剧 → 解说漫规则
    │       ├── 仿真人短剧 → 精品漫规则
    │       └── 2D动态漫剧 → 精品漫规则
    │
    ├── 2. 执行剧本清洗
    │       ├── 场景标注与切分
    │       ├── 角色提取与状态标注
    │       ├── 动作具象化/拆解
    │       ├── 对白/旁白分离
    │       └── 时间轴拆分（精品漫）
    │
    ├── 3. 输出标准格式剧本
    │
    ├── 4. AI拆分镜
    │       └── 根据清洗后剧本拆分镜列表
    │
    └── 5. 生成剧集
            └── 按集数组织分镜
    │
    ▼
【DEMO可见】完成后自动刷新下方剧集面板、分镜列表、视频卡片
```

#### 3.1.4 清洗规则映射

【系统自动】剧本类型与清洗规则对应关系：

| 剧本类型 | 清洗规则 | 规则说明 |
|----------|----------|----------|
| 解说剧 | 解说漫规则 | 场景标注、动作具象化、情绪标注、旁白标记 |
| 仿真人短剧 | 精品漫规则 | 动作拆解、对白提取、时间轴拆分、景别/运镜建议 |
| 2D动态漫剧 | 精品漫规则 | 同上 |

**解说漫清洗规则详述**：
- D-01 场景标注：每段开头标注[场景名]
- D-02 角色提取：提取每段出场角色，标注主/次
- D-03 动作具象化：口语化动作转为可画面化的具象描述
- D-04 情绪标注：在动作后标注角色情绪状态
- D-05 场景切分：按时间/地点变化切分为独立场景段
- D-06 去除心理描写：心理活动转为可表现的动作/表情
- D-07 旁白标记：标注哪些内容为旁白解说

**精品漫清洗规则详述**：
- P-01 场景标注：每段开头标注[场景名+时间]
- P-02 角色提取+状态：提取出场角色，标注情绪状态和位置关系
- P-03 动作拆解：粗略动作拆解为起止清晰的连续动作链
- P-04 对白提取：独立提取对白，标注语气和音量
- P-05 景别建议：根据动作/情绪强度建议景别
- P-06 运镜建议：根据动作走向建议运镜方式
- P-07 时间轴拆分：每段按动作节点拆分为2-3个时间阶段
- P-08 嘴巴状态标注：有对白阶段标注口型状态

---

### 3.2 步骤2：剧集面板

#### 3.2.1 功能描述

【DEMO可见】卡片式展示剧集，支持分页、编辑和删除。剧集与下方分镜表/视频生成强绑定。

#### 3.2.2 页面布局

【DEMO可见】
- 集数标签切换（30集，支持翻页）
- 卡片网格展示，每页10集
- 分页控件

#### 3.2.3 卡片内容

【DEMO可见】

| 字段 | 说明 |
|------|------|
| 集数 | 第N集 |
| 分镜数 | 统计当前集的分镜数量 |
| 场景 | 当前集场景名称 |
| 人物 | 出场人物列表 |
| 摘要 | 剧集内容摘要 |

#### 3.2.4 卡片操作

【DEMO可见】

| 操作 | 说明 |
|------|------|
| 编辑 | 弹出剧集编辑弹窗，修改场景/人物/摘要 |
| 删除 | 删除当前剧集（同时删除关联分镜） |

#### 3.2.5 数据来源

【系统自动】剧集数据由"AI剧本分析"自动生成：
- 根据剧本内容自动切分为N集
- 每集包含场景、人物、摘要等元信息
- 与分镜列表、视频卡片强联动：
  - 点击某集卡片 → 下方分镜列表自动筛选为该集的分镜
  - 点击某集卡片 → 下方视频卡片自动筛选为该集的视频
  - 剧集面板选中状态与分镜列表/视频卡片的集数筛选双向同步
  - 【DEMO可见】当前DEMO中分镜列表和视频区域有集数切换按钮，但未实现与剧集面板卡片的点击联动；PRD要求技术实现时完成此联动

---

### 3.3 步骤3：分镜列表

#### 3.3.1 功能描述

【DEMO可见】表格展示分镜，支持景别/运镜快速修改，资产标签直达编辑。

#### 3.3.2 表格列

【DEMO可见】

| 列名 | 说明 | 可编辑 |
|------|------|--------|
| 镜号 | E-S格式（如1-1表示第1集第1镜） | 只读 |
| 描述 | 分镜内容摘要 | 只读（点击详情可编辑） |
| 景别 | 特写/近景/中景/全景/远景 | 下拉选择 |
| 运镜 | 固定/推镜/拉镜/跟拍/横移/升降/环绕 | 下拉选择 |
| 关联角色 | 角色标签（绿色=已配置，红色=未配置） | 点击标签直达编辑 |
| 关联场景 | 场景标签（可点击直达编辑） | 点击标签直达编辑 |
| 关联道具 | 道具标签（绿色=已配置，红色=未配置） | 点击标签直达编辑 |
| 对白/旁白 | 对话内容摘要 | 只读 |
| 操作 | 详情 / 删除 | - |

#### 3.3.3 核心交互变更

【DEMO可见】
- **操作列**：合并为"详情"一个入口（不再分"查看"和"编辑"两个按钮）
- **资产标签点击**：直接弹出该资产的编辑弹窗，不经过库列表

#### 3.3.4 数据来源

【系统自动】分镜数据由清洗后的剧本拆分生成：
- 镜号：自动编号（集-镜）
- 描述：从清洗后的标准格式剧本填充
- 景别：从清洗结果自动填充（如精品漫的P-05规则）
- 运镜：从清洗结果自动填充（如精品漫的P-06规则）
- 关联角色：从清洗结果的角色提取自动关联
- 关联场景：从清洗结果的场景标注自动关联
- 对白/旁白：从清洗结果的P-04/D-07规则提取

---

### 3.4 步骤4：生成视频

#### 3.4.1 功能描述

【DEMO可见】网格展示分镜视频卡片，支持单条生成、批量生成、查看历史。

#### 3.4.2 卡片结构

【DEMO可见】
```
┌─────────────────────────────────────────┐
│ 镜N                    [状态标签]         │
│ 分镜描述摘要...                            │
│ [角色标签] [场景标签] [道具标签]           │
├─────────────────────────────────────────┤
│                                         │
│            视频预览区                    │
│                                         │
├─────────────────────────────────────────┤
│ [生成视频] / [播放] / [重试]  [历史]     │
└─────────────────────────────────────────┘
```

#### 3.4.3 状态流转

【DEMO可见】

| 当前状态 | 按钮样式 | 操作 |
|----------|----------|------|
| 未生成 | 蓝色渐变"🎬 生成视频" | 点击触发生成 |
| 生成中 | 蓝色边框"生成中..."（脉冲动画） | - |
| 已生成 | 绿色边框"▶ 播放" | 播放视频 |
| 生成失败 | 红色边框"⚠️ 重试" | 点击重试 |

#### 3.4.4 批量操作

【DEMO可见】

| 按钮 | 说明 |
|------|------|
| 批量生成 | 为当前集所有未生成的分镜生成视频 |
| 全部导出 | 导出当前集所有已生成的视频 |

#### 3.4.5 历史记录

【DEMO可见】当某分镜有2条及以上生成记录时，显示"📋 历史"按钮，点击弹出历史记录弹窗。

---

## 四、弹窗系统详述

### 4.1 分镜详情弹窗（detailModal）

#### 4.1.1 核心定位

【DEMO可见】合并查看+编辑+多参配置，一个弹窗一次搞定。

#### 4.1.2 弹窗尺寸

宽度1100px

#### 4.1.3 布局结构

【DEMO可见】
```
┌──────────────────────────────────────────────────────────────────┐
│ 分镜详情                                              [×]        │
├────────────────────────────────┬─────────────────────────────────┤
│  左半区：分镜内容               │  右半区：视频生成参数             │
│  ┌────────────────────────┐    │  ┌─────────────────────────────┐ │
│  │ 镜头号（只读）          │    │  │ 参考图片 (0/3)              │ │
│  ├────────────────────────┤    │  │ [+]                         │ │
│  │ 景别 / 运镜（两列）     │    │  ├─────────────────────────────┤ │
│  ├────────────────────────┤    │  │ 角色（可点击图片直达编辑）  │ │
│  │ 时长/秒                 │    │  │ [选择角色▼] [角色图]        │ │
│  ├────────────────────────┤    │  ├─────────────────────────────┤ │
│  │ 描述（大面积文本框）     │    │  │ 场景（可点击图片直达编辑）  │ │
│  ├────────────────────────┤    │  │ [选择场景▼] [场景图]        │ │
│  │ 对白/旁白               │    │  ├─────────────────────────────┤ │
│  ├────────────────────────┤    │  │ 道具（可点击图片直达编辑）  │ │
│  │ [✓] 有声视频            │    │  │ [选择道具▼] [道具图]        │ │
│  ├────────────────────────┤    │  ├─────────────────────────────┤ │
│  │ 关联角色（标签可点击）   │    │  │ 视频提示词（自动填充）       │ │
│  │ 关联场景（标签可点击）   │    │  ├─────────────────────────────┤ │
│  │ 关联道具（标签可点击）   │    │  │ 模型 / 分辨率（两列）        │ │
│  └────────────────────────┘    │  │ 比例 / 时长（两列）          │ │
│                                │  └─────────────────────────────┘ │
├────────────────────────────────┴─────────────────────────────────┤
│ [保存]                        [查看历史] [生成视频] [关闭]        │
└──────────────────────────────────────────────────────────────────┘
```

#### 4.1.4 左半区分镜内容

【DEMO可见】

| 字段 | 类型 | 说明 |
|------|------|------|
| 镜头号 | 只读输入框 | 格式：E-S |
| 景别 | 下拉选择 | 特写/近景/中景/全景/远景 |
| 运镜 | 下拉选择 | 固定/推镜/拉镜/跟拍/横移/升降/环绕 |
| 时长/秒 | 数字输入 | 默认5 |
| 描述 | 文本框(4行) | 从AI分析自动填充 |
| 对白/旁白 | 文本框(2行) | 角色对话/旁白内容 |
| 有声视频 | 复选框 | 勾选表示有对白/旁白 |
| 关联角色/场景/道具 | 标签组 | 点击标签直达编辑弹窗 |

#### 4.1.5 右半区视频生成参数

【DEMO可见】

| 配置项 | 类型 | 说明 |
|--------|------|------|
| 参考图片 | 上传(0/3) | 点击+号上传，悬浮显示删除按钮 |
| 角色 | 下拉→图片 | 选择角色后展示该角色图，点击可放大/替换 |
| 场景 | 下拉→图片 | 选择场景后展示该场景图，点击可放大/替换 |
| 道具 | 下拉→图片 | 选择道具后展示该道具图，最多2个 |
| 视频提示词 | 文本输入 | 从分镜描述自动填充 |
| 模型 | 下拉选择 | 豆包-Seedance-2.0 / 万向-2.6-图生视频 |
| 分辨率 | 下拉选择 | 720P |
| 比例 | 下拉选择 | 9:16 / 16:9 / 1:1 |
| 时长 | 下拉选择 | 4s / 5s / 6s / 8s / 10s |

#### 4.1.6 多参数据自动映射

【系统自动】多参配置的自动填充逻辑：

| 来源 | 映射到多参配置 | 触发时机 |
|------|----------------|----------|
| 分镜关联角色 | 多参-角色下拉自动选中第一个 | 打开详情弹窗时 |
| 分镜关联场景 | 多参-场景下拉自动选中 | 打开详情弹窗时 |
| 分镜关联道具 | 多参-道具下拉自动选中 | 打开详情弹窗时 |
| 分镜描述 | 多参-视频提示词自动填充 | 打开详情弹窗时 |
| 用户选择的剧本比例 | 多参-比例默认选中 | 打开详情弹窗时 |
| 系统默认值 | 模型=Seedance-2.0, 分辨率=720P, 时长=5s | 始终 |

**【DEMO可见】表现为**：用户打开详情弹窗时，右侧多参配置区域已经"填好"了相关内容。

#### 4.1.7 底部按钮

【DEMO可见】

| 按钮 | 说明 |
|------|------|
| 保存 | 保存左侧分镜内容修改 |
| 查看历史 | 生成2次及以上出现，点击弹出历史记录弹窗 |
| 生成视频 | 主按钮，触发视频生成 |
| 关闭 | 关闭弹窗 |

---

### 4.2 历史记录弹窗（historyModal）

#### 4.2.1 触发场景

【DEMO可见】
- 详情弹窗中生成2次及以上点击"查看历史"
- 视频卡片中某分镜有2条及以上记录时点击"📋 历史"

#### 4.2.2 弹窗尺寸

宽度800px

#### 4.2.3 弹窗内容

【DEMO可见】
```
┌──────────────────────────────────────────────────────────┐
│ 生成历史                                          [×]    │
├──────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────┐  │
│ │ [缩略图] 第1次生成  成功  2026-05-15 14:30        │  │
│ │            [播放] [确认] [删除]                    │  │
│ └────────────────────────────────────────────────────┘  │
│ ┌────────────────────────────────────────────────────┐  │
│ │ [缩略图] 第2次生成  失败  2026-05-15 14:35        │  │
│ │            [重试] [删除]                          │  │
│ └────────────────────────────────────────────────────┘  │
│ ┌────────────────────────────────────────────────────┐  │
│ │ [缩略图] 第3次生成  成功  2026-05-15 14:40        │  │
│ │            [播放] [确认] [删除]                    │  │
│ └────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────┤
│                                            [关闭]       │
└──────────────────────────────────────────────────────────┘
```

#### 4.2.4 记录列表项

【DEMO可见】

| 字段 | 说明 |
|------|------|
| 缩略图 | 视频封面/截图，60×40px |
| 序号 | 第N次生成 |
| 生成时间 | 格式：YYYY-MM-DD HH:mm |
| 状态标签 | 成功（绿色）/ 失败（红色） |

#### 4.2.5 操作按钮

【DEMO可见】

| 状态 | 可用操作 |
|------|----------|
| 成功 | 播放 / 确认（设为最终版）/ 删除 |
| 失败 | 重试 / 删除 |

#### 4.2.6 为何用弹窗而非内联展开

【系统设计说明】
- 生成2条以上时内联展开高度不可控
- 弹窗可独立滚动，体验稳定
- 符合产品一致性原则

---

### 4.3 资产库弹窗（角色库/场景库/道具库）

#### 4.3.1 触发入口

【DEMO可见】顶部栏按钮

#### 4.3.2 弹窗尺寸

宽度900px

#### 4.3.3 卡片网格布局

【DEMO可见】
```
┌─────────┐ ┌─────────┐ ┌─────────┐
│  [图片] │ │  [图片] │ │  [图片] │
│  名称   │ │  名称   │ │  名称   │
│  类型   │ │  类型   │ │  类型   │
│  描述   │ │  描述   │ │  描述   │
│ [编辑]  │ │ [编辑]  │ │ [编辑]  │
└─────────┘ └─────────┘ └─────────┘
```

#### 4.3.4 角色库卡片

【DEMO可见】

| 字段 | 说明 |
|------|------|
| 预览图 | 角色参考图，100%宽度，120px高 |
| 名称 | 角色名称 |
| 类型标签 | 主角（紫色）/ 配角（灰色）/ 龙套（灰色） |
| 描述 | 角色简介 |
| 配置状态 | 绿色=已配置，红色=未配置 |

#### 4.3.5 场景库卡片

【DEMO可见】

| 字段 | 说明 |
|------|------|
| 预览图 | 场景参考图，100%宽度，120px高 |
| 名称 | 场景名称 |
| 时间段标签 | 白天/夜晚/黄昏/黎明（蓝色） |
| 描述 | 场景简介 |

#### 4.3.6 道具库卡片

【DEMO可见】

| 字段 | 说明 |
|------|------|
| 预览图 | 道具参考图，100%宽度，120px高 |
| 名称 | 道具名称 |
| 类别标签 | 武器/服饰/载具/其他（橙色） |
| 描述 | 道具简介 |

---

### 4.4 资产编辑弹窗（角色编辑/场景编辑/道具编辑）

#### 4.4.1 两种入口

【DEMO可见】
1. 资产库弹窗内点击"编辑"
2. **分镜列表/详情弹窗里点击资产标签**（直接弹出，不经过库列表）

#### 4.4.2 角色编辑弹窗

【DEMO可见】

| 字段 | 类型 | 说明 |
|------|------|------|
| 名称 | 文本输入 | 角色名称 |
| 类型 | 下拉选择 | 主角/配角/龙套 |
| 描述 | 文本框 | 角色详细描述 |
| 角色参考图 | 大图展示 | 可放大查看、删除、替换 |
| 重新生成图片 | 按钮 | 触发AI重新生成角色图 |

#### 4.4.3 场景编辑弹窗

【DEMO可见】

| 字段 | 类型 | 说明 |
|------|------|------|
| 名称 | 文本输入 | 场景名称 |
| 时间段 | 下拉选择 | 白天/夜晚/黄昏/黎明 |
| 描述 | 文本框 | 场景详细描述 |
| 场景参考图 | 大图展示 | 可放大查看、删除、替换 |
| 重新生成图片 | 按钮 | 触发AI重新生成场景图 |

#### 4.4.4 道具编辑弹窗

【DEMO可见】

| 字段 | 类型 | 说明 |
|------|------|------|
| 名称 | 文本输入 | 道具名称 |
| 类别 | 下拉选择 | 武器/服饰/载具/其他 |
| 描述 | 文本框 | 道具详细描述 |
| 道具参考图 | 大图展示 | 可放大查看、删除、替换 |
| 重新生成图片 | 按钮 | 触发AI重新生成道具图 |

#### 4.4.5 编辑弹窗操作

【DEMO可见】

| 按钮 | 位置 | 说明 |
|------|------|------|
| 删除 | 左下 | 删除当前资产 |
| 保存 | 右下 | 保存修改 |

---

### 4.5 剧集编辑弹窗

#### 4.5.1 弹窗尺寸

宽度500px

#### 4.5.2 编辑字段

【DEMO可见】

| 字段 | 类型 | 说明 |
|------|------|------|
| 场景 | 文本输入 | 剧集场景名称 |
| 人物 | 文本输入 | 出场人物，逗号分隔 |
| 摘要 | 文本框 | 剧集内容摘要 |

#### 4.5.3 操作按钮

【DEMO可见】

| 按钮 | 位置 | 说明 |
|------|------|------|
| 删除 | 左下 | 删除当前剧集（同时删除关联分镜） |
| 保存 | 右下 | 保存修改 |

---

### 4.6 图片预览遮罩

#### 4.6.1 触发

【DEMO可见】点击任意图片

#### 4.6.2 交互

【DEMO可见】
- 全屏黑色遮罩（95%透明度）
- 图片居中展示，最大90vw×90vh
- 点击遮罩或×按钮关闭

---

## 五、图片交互规范

所有资产图片和参考图片，统一遵循以下交互规范：

### 5.1 交互矩阵

【DEMO可见】

| 操作 | 交互方式 | 视觉反馈 |
|------|----------|----------|
| 放大查看 | 点击图片 | 弹出全屏预览遮罩 |
| 删除 | 悬浮时右上角出现✕按钮 | 点击删除该图片 |
| 替换 | 悬浮时出现↻替换按钮 | 点击重新选择/生成 |
| 重新生成 | 编辑弹窗内的"重新生成图片"按钮 | 触发AI生成，显示loading状态 |

### 5.2 悬浮效果

【DEMO可见】

**资产选择后展示的图片**（角色/场景/道具图）：
- 默认：正常显示，无操作按钮
- 悬浮：显示半透明黑色遮罩 + 🔍放大按钮 + ↻替换按钮
- 图片容器尺寸：80×80px（多参配置区）/ 120-200px（编辑弹窗）

**参考图片上传区**：
- 默认：虚线边框 + + 号
- 悬浮：边框变蓝色
- 上传后：显示缩略图，悬浮显示🔍和✕

### 5.3 资产标签状态

【DEMO可见】

| 状态 | 颜色 | 含义 |
|------|------|------|
| 已配置 | 绿色（#22c55e） | 资产已有参考图 |
| 未配置 | 红色（#ef4444） | 资产尚无参考图 |

---

## 六、多参配置设计

### 6.1 资产选定即出图

#### 6.1.1 核心原则

【系统设计】资产类配置项（角色/场景/道具）选择后直接展示该资产的图片，而不是展示资产列表供选择。

#### 6.1.2 交互流程

【DEMO可见 + 系统自动】

```
1. 用户点击下拉选择框
       │
       ▼
2. 【DEMO可见】展开资产列表（名称列表）
       │
       ▼
3. 用户选择一个资产
       │
       ▼
4. 【系统自动】根据资产ID从资产数据库读取图片URL
       │
       ▼
5. 【DEMO可见】展示该资产的图片
       │
       ▼
6. 【DEMO可见】图片悬浮显示操作按钮（放大/替换）
```

#### 6.1.3 图片展示规则

【DEMO可见 + 系统设计】

| 资产类型 | 展示尺寸 | 数量 |
|----------|----------|------|
| 角色 | 80×80px（多参）/ 150×200px（编辑） | 1张 |
| 场景 | 80×80px（多参）/ 200×133px（编辑） | 1张 |
| 道具 | 80×80px（多参）/ 120×120px（编辑） | 最多2张 |

---

### 6.2 多参数据自动映射

【系统自动】多参配置的自动填充逻辑汇总：

| 分镜数据 | 映射到多参配置 | 说明 |
|----------|----------------|------|
| 分镜关联的角色列表 | 多参-角色下拉自动选中第一个 | 关联关系在拆分镜时建立 |
| 分镜关联的场景 | 多参-场景下拉自动选中 | 通常是1:1关联 |
| 分镜关联的道具列表 | 多参-道具下拉自动选中 | 通常0-2个 |
| 分镜描述 | 多参-视频提示词自动填充 | 内容直接复制 |
| 用户选择的画面比例 | 多参-比例默认值 | 贯穿整个剧本 |
| 系统默认值 | 模型=Seedance-2.0, 分辨率=720P, 时长=5s | 始终保持 |

**【DEMO可见】表现为**：用户打开详情弹窗时，右侧多参配置区域已经"填好"了相关内容。

---

### 6.3 生成机制

#### 6.3.1 单次生成+重试

【DEMO可见 + 系统设计】
- 每次生成1个视频
- 不设"数量"选择（**去掉旧版1-4的抽卡逻辑**）
- 不满意可点击"重新生成"
- 每次重试消耗同等积分，明确提示

#### 6.3.2 视频生成方式

【系统设计】
- **不再需要首帧/首尾帧选择**
- 直接多参生成：角色+场景+道具+提示词 → 视频
- 融图步骤已移除（不需要单独的融图流程）

---

## 七、数据模型

### 7.1 核心数据结构

**剧本（Script）**：
```javascript
{
  id: string,
  title: string,           // 剧本名称
  content: string,          // 原始剧本内容
  type: enum,              // 解说剧/仿真人短剧/2D动态漫剧
  aspectRatio: string,     // 9:16/16:9/1:1
  status: enum,            // 待分析/分析中/已完成
  createdAt: datetime
}
```

**剧集（Episode）**：
```javascript
{
  id: number,
  scriptId: string,
  episodeNumber: number,    // 集数
  scene: string,           // 主要场景
  characters: string,      // 人物（逗号分隔）
  summary: string          // 摘要
}
```

**分镜（Storyboard）**：
```javascript
{
  id: number,
  episodeId: number,
  shotNumber: number,      // 镜号
  scene: string,           // 场景名
  content: string,         // 分镜描述
  characters: string[],    // 关联角色列表
  props: string[],         // 关联道具列表
  dialogue: string,        // 对白/旁白
  shotType: enum,          // 特写/近景/中景/全景/远景
  cameraMove: enum,        // 固定/推镜/拉镜/跟拍/横移/升降/环绕
  duration: number,        // 时长（秒）
  hasAudio: boolean,       // 有声视频开关
  hasVideo: boolean,       // 是否已生成视频
  videoHistory: VideoRecord[]
}
```

**视频记录（VideoRecord）**：
```javascript
{
  id: number,               // 第N次生成
  success: boolean,        // 成功/失败
  videoUrl: string,        // 视频URL
  thumbnailUrl: string,    // 缩略图URL
  params: {                // 生成参数快照
    model: string,
    aspectRatio: string,
    duration: number,
    prompt: string,
    characterIds: string[],
    sceneId: string,
    propIds: string[]
  },
  generatedAt: datetime
}
```

**资产-角色（Character）**：
```javascript
{
  id: string,
  name: string,
  type: enum,              // 主角/配角/龙套
  description: string,
  imageUrl: string,        // 参考图
  configured: boolean     // 是否已配置
}
```

**资产-场景（Scene）**：
```javascript
{
  id: string,
  name: string,
  timeOfDay: enum,         // 白天/夜晚/黄昏/黎明
  description: string,
  imageUrl: string,
  configured: boolean
}
```

**资产-道具（Prop）**：
```javascript
{
  id: string,
  name: string,
  category: enum,          // 武器/服饰/载具/其他
  description: string,
  imageUrl: string,
  configured: boolean
}
```

### 7.2 状态定义

**资产配置状态**：
| 值 | 标签颜色 | 含义 |
|----|----------|------|
| true | 绿色 | 已配置参考图 |
| false | 红色 | 未配置参考图 |

**视频生成状态**：
| 值 | 状态标签 | 按钮 |
|----|----------|------|
| 待生成 | 灰色 | "🎬 生成视频" |
| 生成中 | 蓝色动画 | "生成中..." |
| 已生成 | 绿色 | "▶ 播放" |
| 失败 | 红色 | "⚠️ 重试" |

---

## 八、非功能性需求

### 8.1 性能需求

【系统设计】

| 需求项 | 目标 |
|--------|------|
| 页面加载 | 首屏加载时间≤2秒 |
| 弹窗响应 | 弹窗打开/关闭≤200ms |
| 分镜列表渲染 | 30集×6分镜/集=180条数据，渲染时间≤500ms |
| 图片预览 | 点击图片到预览遮罩展示≤300ms |
| 视频生成 | 单次生成≤60秒 |

### 8.2 兼容性需求

【系统设计】

| 需求项 | 说明 |
|--------|------|
| 浏览器支持 | Chrome/Firefox/Safari/Edge 最新版 |
| 响应式 | 最小宽度1280px |
| 移动端 | 暂不支持（工作流工具以PC为主） |

### 8.3 清洗质量需求

【系统设计】

| 需求项 | 目标 |
|--------|------|
| 清洗进度 | 需展示进度提示（正在清洗剧本...→正在拆分镜...→分析完成！） |
| 动作保留率 | 清洗后剧本的动作保留率应≥90% |
| 分镜准确性 | 分镜与清洗后剧本的对应准确率应≥85% |

### 8.4 视频生成需求

【系统设计】

| 需求项 | 目标 |
|--------|------|
| 生成成功率 | 视频生成成功率应≥70% |
| 并发支持 | 支持同时生成3个视频任务 |
| 重试机制 | 生成失败时保留参数配置，支持一键重试 |
| 成本透明 | 生成前明确提示本次消耗积分 |
| 重试上限 | 单分镜建议最多重试5次 |

---

## 九、风险与依赖

### 9.1 风险项

【系统设计】

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 清洗规则需持续调优 | 清洗质量不稳定，影响分镜准确性 | 首期支持解说漫和精品漫，建立规则迭代机制，支持用户手动修正 |
| 单次生成+重试可能导致积分消耗不可预期 | 用户反复重试积分消耗大 | 生成前明确提示积分消耗，设置单分镜重试上限（建议5次） |
| 资产选定即出图依赖资产数据完整性 | 资产未配置时无法展示图片 | 标签状态区分已配置/未配置，引导用户完善资产 |

### 9.2 依赖项

【系统设计】

| 依赖 | 说明 |
|------|------|
| 剧本清洗Skills/MCP | 清洗规则实现，清洗在后台自动完成 |
| 视频生成API | 图生视频/参考生视频能力 |
| 资产图片存储 | 角色/场景/道具参考图的存储与访问 |

---

## 十、里程碑

| 阶段 | 内容 | 优先级 | 标签 | 备注 |
|------|------|--------|------|------|
| P0 | 4步流程框架搭建 | 高 | 【DEMO可见】 | 原始剧本→剧集面板→分镜列表→生成视频 |
| P0 | 详情弹窗（detailModal）合并查看+编辑 | 高 | 【DEMO可见】 | 一个弹窗搞定分镜编辑+多参配置 |
| P0 | 资产选定即出图 | 高 | 【DEMO可见+系统自动】 | 选择角色/场景/道具后直接展示图片 |
| P0 | 生成历史弹窗化 | 高 | 【DEMO可见】 | historyModal，2条以上不会撑开高度 |
| P0 | 资产标签点击直达编辑 | 高 | 【DEMO可见】 | 不经过库列表，直接弹出编辑弹窗 |
| P0 | 资产编辑弹窗-重新生成图片 | 高 | 【DEMO可见+系统自动】 | 角色/场景/道具编辑弹窗内含重新生成按钮 |
| P1 | 剧本清洗后台自动 | 中 | 【系统自动】 | 去掉用户预览确认步骤，后台自动完成 |
| P1 | 批量生成视频 | 中 | 【DEMO可见】 | 当前集所有未生成分镜一键生成 |
| P2 | 清洗规则自学习 | 低 | 【系统自动】 | 根据用户修正记录优化规则 |

---

## 附录A：功能可见性分类汇总

### A.1 【DEMO可见】功能清单

| 功能模块 | 具体功能 |
|----------|----------|
| 原始剧本 | 剧本展示区、类型/比例下拉、AI分析按钮、进度动画 |
| 剧集面板 | 集数标签、卡片展示、分页控件、编辑/删除按钮 |
| 分镜列表 | 9列表格、景别/运镜下拉、资产标签、操作按钮 |
| 生成视频 | 视频卡片网格、状态标签、操作按钮、批量操作 |
| detailModal | 分镜内容编辑区、多参配置区、资产图片展示、底部按钮 |
| historyModal | 生成记录列表、操作按钮 |
| 资产库弹窗 | 角色库/场景库/道具库卡片网格 |
| 资产编辑弹窗 | 角色/场景/道具编辑表单、参考图展示、重新生成按钮 |
| 剧集编辑弹窗 | 场景/人物/摘要编辑表单 |
| 图片预览 | 全屏预览遮罩 |
| 图片交互 | 悬浮显示放大/删除/替换按钮 |

### A.2 【系统自动】功能清单

| 功能模块 | 具体逻辑 |
|----------|----------|
| 剧本清洗 | 规则映射、场景标注、角色提取、动作具象化、对白分离等 |
| 剧集生成 | 根据剧本自动切分为N集 |
| 分镜拆分 | 根据清洗后剧本自动拆分镜列表 |
| 数据填充 | 景别/运镜/角色/场景/道具关联自动建立 |
| 多参映射 | 分镜数据自动映射到多参配置（角色/场景/道具/提示词/比例） |
| 默认值 | 模型/分辨率/时长的默认值填充 |
| 图片读取 | 资产选择后从数据库读取图片URL |

### A.3 【DEMO模拟】功能清单

| 功能模块 | 模拟方式 |
|----------|----------|
| 剧本数据 | 使用《穿成霍去病》剧本作为示例 |
| 剧集/分镜数据 | 30集×6分镜的模拟数据 |
| 角色/场景/道具数据 | 预置模拟资产数据 |
| 视频生成 | 点击按钮后用setTimeout模拟生成过程 |
| 生成历史 | 部分分镜预置2-3条模拟历史记录 |
| 图片 | 使用picsum.photos模拟图片URL |

---

## 附录B：变更摘要

| 变更项 | v1.3（6步） | v12.1（4步） |
|--------|------------|--------------|
| 步骤数量 | 6步（剧本列表→剧本解析→解析详情→分镜列表→融图管理→生成视频） | 4步（原始剧本→剧集面板→分镜列表→生成视频） |
| 融图步骤 | 独立步骤 | **移除**（不需要融图流程） |
| 分镜图片步骤 | 独立步骤 | **移除**（不需要单独的分镜图片步骤） |
| 视频生成方式 | 首帧/首尾帧选择 | **直接多参生成** |
| 剧本清洗 | 需用户预览确认 | **后台自动完成** |
| 分镜编辑 | 查看弹窗+编辑弹窗（两个） | detailModal（一个弹窗搞定） |
| 操作列入口 | 查看\|编辑\|删除 | 详情\|删除 |
| 资产选择 | 从列表中选择 | 选定即出图（可放大/删除/替换） |
| 资产编辑入口 | 需进入库列表 | 分镜列表标签点击直达 |
| 生成历史 | 内联展开 | historyModal弹窗 |
| 多参数据来源 | 无明确来源 | 从分镜关联标签自动映射 |
| 编辑弹窗功能 | 仅编辑 | 编辑+重新生成图片 |
